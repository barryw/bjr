en:
  common:
    errors:
      invalid_timezone: "Invalid timezone '%{timezone}' specified. Acceptable timezone list: %{timezone_list_url}"
      server_error: "A server error has occurred. Use the error ID '%{errorid}' to track it down."
  tags:
    messages:
      received: Tags received successfully.
  timezones:
    messages:
      received: Timezones received successfully.
  occurrences:
    messages:
      received: Job occurrences received successfully.
  jobruns:
    messages:
      received: Job runs received successfully.
  jobs:
    messages:
      received: Job(s) received successfully.
      created: Job %{id} created successfully.
      deleted: Job %{id} deleted successfully.
      updated: Job %{id} updated successfully.
      started: Job %{id} queued to run.
      runs: Runs for job %{id}
    errors:
      not_found: Job not found.
      create_failed: "Failed to create job: %{error}"
      update_failed: "Failed to update job '%{id}': %{error}"
      already_exists: A job with this name already exists.
      end_date_required: You must supply an end date parameter.
      cant_delete_running: "Job '%{id}' cannot be deleted because it's running. Try disabling it first."
      already_running: "Job '%{id}' is already running."
  users:
    messages:
      received: User(s) received successfully.
      created: User %{id} created successfully.
      deleted: User %{id} deleted successfully.
      updated: User %{id} updated successfully.
    errors:
      not_found: User not found.
      update_failed: "Failed to update user '%{id}': %{error}"
      already_exists: A user with this username already exists.
      cant_delete_yourself: You cannot delete yourself.
      invalid_credentials: Invalid credentials
      not_authorized: Not authorized
      invalid_token: Invalid token
      missing_token: Missing token
  version:
    messages:
      received: Version received successfully.
  jobserver:
    messages:
      todays_stats:
        received: Today's Stats received successfully.
      recent_jobs:
        received: Recently run jobs received successfully.
        failed: The 'count' parameter must be less than 20.
      upcoming_jobs:
        received: Upcoming jobs received successfully.
        failed: The 'count' parameter must be less than 20.
      minutely_job_stats:
        received: Minutely job stats received successfully.
      hourly_job_stats:
        received: Hourly job stats received successfully.
      daily_job_stats:
        received: Daily job stats received successfully.
      weekly_job_stats:
        received: Weekly job stats received successfully.
